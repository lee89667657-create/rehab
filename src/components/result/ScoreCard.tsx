/**
 * ScoreCard.tsx
 * 운동 점수 카드 컴포넌트
 * - 운동 수행 점수 시각화 (100점 만점)
 * - 정확도, 일관성, 범위 등 세부 점수 표시
 * - 등급 표시 (A, B, C, D, F)
 * - 애니메이션 효과로 점수 카운트업
 */

export default function ScoreCard() {
  return (
    <div>
      {/* TODO: 점수 카드 UI 구현 */}
    </div>
  );
}
