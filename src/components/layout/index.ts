/**
 * 레이아웃 컴포넌트 통합 export
 *
 * 이 파일에서 모든 레이아웃 컴포넌트를 한 곳에서 import할 수 있습니다.
 *
 * @example
 * import { Header, BottomNav } from '@/components/layout';
 */

export { default as Header } from './Header';
export { default as BottomNav } from './BottomNav';
